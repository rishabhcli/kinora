// Migrated to ../reading/ReadingRoom by Agent 10 (the book-open film experience).
// This re-export keeps existing imports — HomePage.tsx (Agent 4) does
// `import ReadingRoom from "./ReadingRoom"` — resolving unchanged.
// See coordination/CONTRACTS.md + coordination/requests/agent-10.md.
export { default } from "../reading/ReadingRoom";
