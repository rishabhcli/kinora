# Local override kept aligned with the committed local-dev contract: host
# Postgres listens on :5433 while in-cluster services use postgres:5432.
services:
  postgres:
    ports: !override
      - "5433:5432"
