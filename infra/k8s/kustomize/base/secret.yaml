apiVersion: v1
kind: Secret
metadata:
  name: kinora-secret
type: Opaque
# Placeholder values ONLY. In prod replace with an External Secrets Operator
# ExternalSecret or a Vault-synced Secret — never commit real credentials.
stringData:
  DATABASE_URL: "postgresql+asyncpg://kinora:kinora@kinora-postgres:5432/kinora"
  REDIS_URL: "redis://kinora-redis:6379/0"
  DASHSCOPE_API_KEY: "test"
  JWT_SECRET: "change-me-in-prod-this-must-be-a-real-secret-32b+"
  MCP_AUTH_TOKEN: "change-me-in-prod-mcp-bearer"
