apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kinora.configMapName" . }}
  labels:
    {{- include "kinora.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
