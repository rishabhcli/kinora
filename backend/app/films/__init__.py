"""Film API: the event/scene film HTTP boundary and sync-map contract.

Pure wire models + builders live in :mod:`app.films.contract`; the FastAPI
routes that serve them live in :mod:`app.api.routes.films`.
"""
