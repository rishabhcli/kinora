# Pointer — Agent 02 artifacts live in `../agent-02/`

The Agent 02 (Scroll Film Engine) mission's DoD says "Artifacts in
`coordination/artifacts/agent-04/`" and "Shared seams → `requests/agent-04.md`".
Those `agent-04` paths are template residue — agent 04 is the motion-animation
agent. Agent 02's real artifacts are in **`coordination/artifacts/agent-02/`**:

- `DESIGN.md` — design + decisions
- `VERIFICATION.md` — DoD → evidence, peer-review outcome, scope notes
- `verify-output.txt` — the runtime verifier output (14/14)
- `PROGRESS.md` — durable progress tracker

The cross-seam request file was created at the literal path
`coordination/requests/agent-04.md` (as the mission instructed).

(This breadcrumb is uniquely named so it won't conflict with the motion agent's
own `agent-04/` artifacts at merge.)
